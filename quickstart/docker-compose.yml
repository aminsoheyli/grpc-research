version: '3.8'
services:
  server:
    build: ./server
    ports:
      - "8080:8080"
    restart: always
  client:
    build: ./client
    restart: always
    environment:
      - SERVER_HOST=server
      - SERVER_PORT=8080
    depends_on:
      - server